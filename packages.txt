# System packages for Linux deployment
# These are apt packages that will be installed on the deployment server

# Image processing dependencies
libjpeg-dev
libpng-dev
libtiff-dev
libwebp-dev
libopenjp2-7-dev

# Font support
fonts-liberation
fontconfig

# Build tools (if needed)
build-essential
pkg-config

# Optional: for better matplotlib support
libfreetype6-dev
libfontconfig1-dev 